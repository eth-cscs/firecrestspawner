name: Bandit Security Scan

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  bandit:
    name: Run Bandit
    runs-on: ubuntu-latest
    permissions:
      contents: read   # only needs read access
      security-events: write   # needed to upload SARIF

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Bandit
        run: pip install bandit

      - name: Run Bandit (JSON output)
        run: bandit -r . -f json -o bandit-results.json

      - name: Convert JSON → SARIF
        uses: microsoft/sarif-tools@v2
        with:
          args: convert bandit-results.json --output bandit-results.sarif

      - name: Upload SARIF to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: bandit-results.sarif
