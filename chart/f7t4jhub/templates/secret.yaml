apiVersion: v1
kind: Secret
metadata:
    name: {{ .Release.Name }}-secret
type: Opaque
stringData:
    firecrestUrl: {{ .Values.setup.firecrestUrl }}
    authTokenUrl: {{ .Values.setup.authTokenUrl}}
    configproxyAuthToken: {{ randAlphaNum 40 | b64enc }}
